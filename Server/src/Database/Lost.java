package Database;

import java.util.HashMap;

// << Lost Database >>
public class Lost {
    // < Attributes >
    HashMap<String, String> lostDataMap = new HashMap<>();
    // lsn, vsn     = Key data
    // date, type   = Type data
}