package Database;

import java.util.HashMap;

// << Vehicle Database >>
public class Vehicle {
    // < Attributes >
    HashMap<String, String> vehicleDataMap = new HashMap<>();
    // vsn, psn         = Key data
    // state, num, pos  = Vehicle data
}

