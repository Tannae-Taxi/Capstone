package Database;

import java.util.HashMap;

// << History Database >>
public class History {
    // < Attributes >
    HashMap<String, String> historyDataMap = new HashMap<>();
    // hsn, usn, vsn        = Key data
    // xpath, ypath, path   = Path data
    // date, cost           = ETC data
}